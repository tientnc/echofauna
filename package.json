{
  "name": "echofauna",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:smoke": "playwright test",
    "check": "npm run test && npm run build && npm run test:smoke"
  },
  "dependencies": {
    "tone": "^15.1.22"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.1",
    "@types/node": "^24.1.0",
    "jsdom": "^26.1.0",
    "typescript": "~5.8.3",
    "vite": "^7.0.6",
    "vitest": "^3.2.4"
  }
}
